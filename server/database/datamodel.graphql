type PhoneNumber {
  id: ID! @unique
  phoneNumber: String!
  label: String!
  contact: Contact!
}

type Contact {
  id: ID! @unique
  firstName: String!
  lastName: String!
  phoneNumbers: [PhoneNumber!]!
}
