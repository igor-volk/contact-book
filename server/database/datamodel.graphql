type PhoneNumber {
  id: ID! @unique
  phoneNumber: String!
  label: String!
  contact: Contact @relation(name: "ContactPhoneNumber")
}

type Contact {
  id: ID! @unique
  firstName: String!
  lastName: String!
  phoneNumbers: [PhoneNumber!]! @relation(name: "ContactPhoneNumber")
}
